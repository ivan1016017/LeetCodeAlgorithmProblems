name: Build
on:
  push:
    branches:
      - main
  pull_request:
    branches:
      -master
    types: [opened, synchronize, reopened]
jobs:
  sonarcloud:
    name: SonarCloud
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
        with:
          fetch-depth: 0
      - name: Setup Python
        uses: actions/setup-python@v2
        with:
          python-version: '3.10.0'
      - name: Instalacion de librerias y dependencias
        run: | 
          python -m pip intall --upgrade pip
          pip install -r requirements.txt 
      - name: SonarCloud Scan
        uses: SonarSource/sonarcloud-github-action@master
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
      - name: pruebas+coverage
        id: pruebas
        run: | 
          python -m coverage run -m unittest discover tests 
          python -m coverage xml -i 
          pytest pytest -v  /home/ivan/WindowsBackup/LeetCodeAlgorithmProblems/src/my_project/easy_problems/from401to450/test