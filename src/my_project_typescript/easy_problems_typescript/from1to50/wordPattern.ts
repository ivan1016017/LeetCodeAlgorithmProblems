function wordPattern(pattern: string, s: string): boolean {


    for (var i = 0; i < pattern.length; i++) {
        console.log(i)
    }
    return true;
};

const myString: string = "this is the first string"
const mySecondString: string = 'this is the second string'
wordPattern(myString, mySecondString);